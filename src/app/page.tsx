import { CommandeeringInterface } from "@/components/commandeering-interface";

export default function Home() {
  return (
    <main className="min-h-screen bg-background text-foreground flex flex-col">
      <CommandeeringInterface />
    </main>
  );
}
